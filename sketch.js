var canvas;
var gamestate = 0;
var playerCount;
var database;
var form;
var player;
var game;

function setup(){
  canvas =createcanvas(800,400);
  database = firebase.database();
}

function draw(){
  
}
